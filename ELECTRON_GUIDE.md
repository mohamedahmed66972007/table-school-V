# دليل تحويل التطبيق إلى برنامج سطح المكتب

## نظرة عامة
تم تجهيز هذا التطبيق ليعمل كبرنامج سطح مكتب باستخدام **Electron**. هذا الدليل سيساعدك خطوة بخطوة لبناء ملف تنفيذي (`.exe` لـ Windows، `.dmg` لـ macOS، `.AppImage` لـ Linux).

---

## ✅ الخطوة 1: التحضيرات الأولية

### 1.1 تثبيت Node.js
تأكد من تثبيت Node.js الإصدار 18 أو أحدث على جهازك:
```bash
node --version
```

إذا لم يكن مثبتاً، قم بتحميله من: https://nodejs.org

### 1.2 تثبيت المكتبات
افتح Terminal (موجه الأوامر) في مجلد المشروع وقم بتشغيل:
```bash
npm install
```

---

## 🎨 الخطوة 2: إضافة أيقونة التطبيق (اختياري لكن مهم)

### لماذا الأيقونة مهمة؟
الأيقونة تظهر على:
- أيقونة البرنامج في سطح المكتب
- شريط المهام (Taskbar)
- قائمة البرامج المثبتة
- ملف التثبيت (Installer)

### كيفية إضافة الأيقونة:

#### لنظام Windows (.ico):
1. احصل على صورة بحجم 256×256 بكسل على الأقل (PNG أو JPG)
2. استخدم موقع مثل https://convertio.co/png-ico/ لتحويلها إلى `.ico`
3. ضع الملف في مجلد `build/` واسمه `icon.ico`

#### لنظام macOS (.icns):
1. احصل على صورة بحجم 512×512 بكسل على الأقل
2. استخدم أداة مثل https://cloudconvert.com/png-to-icns
3. ضع الملف في مجلد `build/` واسمه `icon.icns`

#### لنظام Linux (.png):
1. احصل على صورة PNG بحجم 512×512 بكسل
2. ضع الملف في مجلد `build/` واسمه `icon.png`

**ملاحظة**: إذا لم تضع الأيقونات، سيستخدم Electron الأيقونة الافتراضية.

---

## 🔨 الخطوة 3: بناء التطبيق

### 3.1 اختبار التطبيق في وضع التطوير (اختياري)
قبل البناء، يمكنك تجربة التطبيق محلياً:
```bash
npm run electron:dev
```

**ملاحظة مهمة**: 
- التطبيق سيقوم بتشغيل الـ server تلقائياً عند فتحه
- لا تحتاج لتشغيل `npm run dev` بشكل منفصل
- سيفتح التطبيق في نافذة Electron بعد ثوانٍ قليلة
- للإغلاق، أغلق النافذة أو اضغط `Ctrl+C` في Terminal

### 3.2 بناء التطبيق النهائي

#### لنظام Windows (ملف .exe):
```bash
npm run electron:build:win
```

سيتم إنشاء:
- ملف Installer في: `release/نظام جدولة الحصص Setup X.X.X.exe`
- يمكن توزيع هذا الملف على أي جهاز Windows

#### لنظام macOS:
```bash
npm run electron:build:mac
```

سيتم إنشاء ملف `.dmg` في مجلد `release/`

#### لنظام Linux:
```bash
npm run electron:build:linux
```

سيتم إنشاء ملف `.AppImage` في مجلد `release/`

#### لجميع الأنظمة (Build شامل):
```bash
npm run electron:build
```

---

## 📦 الخطوة 4: توزيع التطبيق

### على Windows:
1. انتقل إلى مجلد `release/`
2. ستجد ملف مثل: `نظام جدولة الحصص Setup 1.0.0.exe`
3. يمكنك نسخ هذا الملف ومشاركته مع أي شخص
4. المستخدم يقوم بـ:
   - تشغيل الملف
   - اتباع خطوات التثبيت
   - بعد التثبيت، سيجد اختصاراً على سطح المكتب

### على macOS:
1. انتقل إلى مجلد `release/`
2. ستجد ملف `.dmg`
3. المستخدم يقوم بفتح الملف وسحب التطبيق إلى مجلد Applications

### على Linux:
1. انتقل إلى مجلد `release/`
2. ستجد ملف `.AppImage`
3. المستخدم يحتاج فقط تشغيله (قد يحتاج إعطائه صلاحية التنفيذ: `chmod +x file.AppImage`)

---

## 🚀 ميزات التطبيق بعد التحويل

### ✅ ما يعمل:
- التطبيق يعمل بدون إنترنت بعد التثبيت
- جميع البيانات محفوظة محلياً
- واجهة مستقلة عن المتصفح
- أداء أسرع من نسخة الويب
- لا يحتاج Node.js مثبت على جهاز المستخدم النهائي

### ⚙️ كيفية عمل التطبيق:
- **Development Mode**: يستخدم `tsx` لتشغيل TypeScript مباشرة
- **Production Mode**: يستخدم Electron's Node.js المدمج لتشغيل الـ server (عبر `fork()`)
- الـ server يعمل تلقائياً عند تشغيل التطبيق على منفذ 5000 محلياً
- النافذة تتصل بالـ server المحلي تلقائياً

### ⚠️ ملاحظات مهمة:
- البيانات تُحفظ في الذاكرة (MemStorage) - تُفقد عند إغلاق البرنامج
- إذا أردت حفظ دائم، يجب تفعيل قاعدة بيانات SQLite أو ملفات JSON
- التطبيق المبني لا يعتمد على Node.js خارجي - كل شيء مدمج

---

## 🔧 تخصيص متقدم

### تغيير اسم التطبيق:
افتح `package.json` وغير:
```json
"productName": "نظام جدولة الحصص"
```

### تغيير نافذة التطبيق:
افتح `electron/main.cjs` وعدّل:
```javascript
const mainWindow = new BrowserWindow({
  width: 1400,     // عرض النافذة
  height: 900,     // ارتفاع النافذة
  minWidth: 1000,  // الحد الأدنى للعرض
  minHeight: 700   // الحد الأدنى للارتفاع
});
```

### إضافة قائمة (Menu):
يمكنك إضافة قائمة مخصصة في `electron/main.cjs`:
```javascript
const { Menu } = require('electron');

const template = [
  {
    label: 'ملف',
    submenu: [
      { role: 'quit', label: 'خروج' }
    ]
  }
];

const menu = Menu.buildFromTemplate(template);
Menu.setApplicationMenu(menu);
```

---

## 📊 حجم التطبيق المتوقع

- Windows: حوالي 150-200 ميجابايت
- macOS: حوالي 180-230 ميجابايت
- Linux: حوالي 160-210 ميجابايت

الحجم كبير لأن Electron يحزّم متصفح Chromium كاملاً مع التطبيق.

---

## 🐛 حل المشاكل الشائعة

### المشكلة: "electron command not found"
**الحل**: قم بتشغيل `npm install` مرة أخرى

### المشكلة: فشل البناء على Windows
**الحل**: تأكد من تشغيل Terminal كمسؤول (Administrator)

### المشكلة: التطبيق لا يفتح بعد البناء
**الحل**: 
1. تأكد من بناء المشروع أولاً: `npm run build`
2. ثم قم ببناء Electron: `npm run electron:build:win`

### المشكلة: البيانات تختفي بعد إغلاق البرنامج
**الحل**: هذا متوقع لأن التطبيق يستخدم MemStorage. لحفظ البيانات بشكل دائم:
1. يمكنك تعديل `server/storage.ts` لاستخدام SQLite أو ملفات JSON
2. أو استخدام electron-store للحفظ المحلي

---

## 📝 خطوات سريعة (ملخص)

```bash
# 1. تثبيت المكتبات
npm install

# 2. (اختياري) إضافة أيقونة في build/icon.ico

# 3. بناء التطبيق
npm run electron:build:win

# 4. ستجد الملف التنفيذي في مجلد release/
```

---

## 📞 دعم إضافي

إذا واجهت أي مشاكل:
1. تأكد من تحديث Node.js إلى أحدث إصدار
2. احذف مجلد `node_modules` وأعد التثبيت: `rm -rf node_modules && npm install`
3. راجع سجلات الأخطاء (Logs) في Terminal

---

## 🎉 مبروك!

أصبح لديك الآن تطبيق سطح مكتب جاهز للتوزيع والاستخدام! 🚀
